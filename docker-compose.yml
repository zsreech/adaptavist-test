# To execute this docker-compose yml file use `docker-compose -f <file_name> up`
# Add the `-d` flag at the end for detached execution
version: '2'  services:   chrome:     image: selenium/standalone-chrome-debug:3.141.59     ports:       - 4444:4444       - 5900:5900     environment:       - SCREEN_WIDTH=1920       - SCREEN_HEIGHT=1080       - SCREEN_DEPTH=8       - DBUS_SESSION_BUS_ADDRESS=/dev/null     volumes:         - /dev/shm:/dev/shm